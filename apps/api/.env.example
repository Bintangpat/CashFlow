# Server Configuration
PORT=3001
NODE_ENV=production

# Database
# For Railway: PostgreSQL service will auto-provide DATABASE_URL
# For local: Use Docker PostgreSQL
DATABASE_URL="postgresql://postgres:postgres123@localhost:5432/cashflow"

# JWT Authentication
# IMPORTANT: Use strong secret (min 32 characters) in production!
JWT_SECRET="your-super-secret-jwt-key-change-in-production-min-32-chars"
JWT_EXPIRES_IN="7d"

# Email Service (Resend)
# Get API key from: https://resend.com/api-keys
RESEND_API_KEY="re_xxxxxxxx"
EMAIL_FROM="CashFlow <noreply@yourdomain.com>"

# CORS Configuration
# Comma-separated list of allowed origins (no spaces recommended)
# For production: Use your actual frontend URLs
# For development: Defaults to localhost:3000,localhost:3001 if not set
# Example: CORS_ORIGINS=https://your-app.vercel.app,https://www.your-app.com
CORS_ORIGINS="https://your-app.vercel.app"

# CORS Credentials (default: true)
# Set to 'false' to disable credentials (cookies, auth headers)
CORS_CREDENTIALS="true"
